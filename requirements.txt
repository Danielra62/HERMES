# =============================================================
#  HERMES — requirements.txt
#  Instalar con: pip install -r requirements.txt
# =============================================================

# Interfaz gráfica del admin (ventana HTML/CSS/JS embebida)
pywebview==5.3.2


# En caso de tener problemas iniciando el admin, verifique si está instalado el WebView2, deberías encontrarlo como "Instalador independiente Evergreen"